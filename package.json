{
  "name": "school-timetable",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "週・月表示の時間割 PWA と、授業10分前の持ち物プッシュ通知を送る Cloudflare Worker",
  "scripts": {
    "dev": "wrangler dev",
    "check": "node tools/check-config.mjs",
    "predeploy": "node tools/check-config.mjs",
    "deploy": "wrangler deploy",
    "secrets": "node tools/check-config.mjs && node tools/put-secrets.mjs",
    "db:init": "wrangler d1 execute timetable --remote --file=schema.sql",
    "db:init:local": "wrangler d1 execute timetable --local --file=schema.sql",
    "db:reset": "wrangler d1 execute timetable --remote --command \"DROP TABLE IF EXISTS schedules; DROP TABLE IF EXISTS devices; DROP TABLE IF EXISTS groups;\"",
    "keys": "node tools/generate-vapid-keys.mjs",
    "icons": "python3 tools/generate-icons.py",
    "test": "node tools/test-webpush.mjs && node tools/test-ical.mjs && node tools/test-dayplan.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
