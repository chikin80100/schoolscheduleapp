# 学校時間割アプリ

週表示 / 月表示を切り替えられる時間割の PWA と、**次の授業の 10 分前に教科と持ち物をプッシュ通知**する
Cloudflare Worker。iPad でホーム画面に追加すると、ネイティブアプリのように使えます。

HTML / CSS / JavaScript のみで実装しており、ビルドツールもフロントエンドの依存パッケージもありません。

## 時程

| 時限 | 時刻 | 通知 |
|---|---|---|
| 1 | 8:50–9:40 | 8:40 |
| 2 | 9:50–10:40 | 9:40 |
| 3 | 10:50–11:40 | 10:40 |
| 4 | 11:50–12:40 | 11:40 |
| 昼休み | 12:40–13:20 | — |
| 5 | 13:20–14:10 | 13:10 |
| 6 | 14:20–15:10 | 14:10 |
| 7（火・木のみ） | 15:20–16:10 | 15:10 |

月・水・金は 6 時限、火・木は 7 時限。授業 50 分・休憩 10 分・4 限と 5 限の間は 40 分の昼休み。
通知の「何分前」は設定画面から 1〜60 分の間で変えられます。

## 主な機能

- **週表示 / 月表示の切り替え** — 週は時限 × 曜日のグリッド、月はカレンダー
- **任意の予定** — 体育祭や三者面談などを日付に登録でき、月表示に出ます（授業は月表示に出しません）。
  日をタップすると、予定の追加・編集・削除と、その日の授業の確認・休講の設定ができます
- **教科カードのドラッグ登録** — 専攻ごとのプリセットをコマにドラッグ。コマ同士のドラッグで入れ替え、
  ゴミ箱にドロップで削除。iPad でも動くよう Pointer Events で自前実装しています
  （タップで選んでコマをタップする置き方もできます）
- **カードの編集** — 持ち物（初期状態は空）・教室・先生・色。持ち物はそのまま通知本文に載ります
- **日付ごとの上書き** — 週テンプレートを基本にしつつ、特定の日だけ休講・変更にできます
- **複数端末で同期** — 同期コードを入れるだけで、iPad と iPhone などが同じ時間割になります
- **プッシュ通知** — Cloudflare Worker の Cron Trigger が毎分チェックし、該当する端末にだけ送信。
  同期グループの端末すべてに届きます
- **オフライン対応** — Service Worker がアプリシェルをキャッシュ。時間割は端末内 (localStorage) に保存

## 専攻と教科

- **普通科目** — 数学Ⅲ / 数学C / 英語コミュニケーション / 英語演習 / 地学基礎 / 生物基礎 / 物理 /
  論理国語 / 国語表現 / 歴史総合 / 政治・経済 / フードデザイン / 体育 / 美術表現 / プロゼミ２ / LHR / 実習
- **機械加工専攻** — 機械設計 / 技術者入門 / 原動機 / 機械工作 / 製図 / 生産技術
- **ロボット専攻** — 電子機械 / 技術者入門 / ハードウェア技術 / 機械工作 / ロボット工学 / 機械設計
- **電気専攻** — 電力技術 / 電気回路 / 電気実習 / 電力技術Ｂ / 電子技術 / 電気製図 / 電気機器 / 電気回路Ｂ
- **電子情報専攻** — 電気回路 / 電子回路 / ハードウェア技術 / 通信応用 / 通信技術 / プログラミング技術

科目は `public/js/subjects.js` の `PRESET_NAMES` にまとまっています。追加・変更はこの 1 ファイルだけで済みます。
専門科目は専攻ごとに色相をそろえ、17 科目ある普通科目は科目ごとに色相を散らしています。

## セットアップ

前提:

- Cloudflare アカウント（無料プランで足ります）
- Node.js 18 以上
- iPadOS 16.4 以上の iPad（通知を使う場合。これ未満では Web Push が使えません）

```bash
git clone https://github.com/chikin80100/schoolscheduleapp.git
cd schoolscheduleapp
npm install
npx wrangler login
```

### 1. D1 データベースを作る

```bash
npx wrangler d1 create timetable
```

表示された `database_id`（`8f2c1a4b-9e3d-4f7a-b1c2-3d4e5f6a7b8c` のような 36 文字）を
`wrangler.toml` の `REPLACE_WITH_YOUR_D1_DATABASE_ID` に書き込みます。**この置き換えを忘れると、
デプロイも鍵の登録も失敗します。**

書き換えたか自信がないときは、次で確認できます。

```bash
npm run check
```

### 2. 連絡先を書き換える

`wrangler.toml` の `VAPID_SUBJECT` を自分の連絡先（`mailto:` か `https:`）にします。
Web Push の仕様で必須の項目です。`[vars]` なので、後から変える場合は再デプロイが要ります。

### 3. デプロイする

```bash
npm run deploy
```

初回は workers.dev のサブドメイン登録を聞かれることがあります。完了すると
`https://school-timetable.<サブドメイン>.workers.dev` の形の URL が表示されるので、控えておきます。
iPad で開くのはこの URL です。

### 4. テーブルを作る

```bash
npm run db:init
```

`wrangler d1 execute` は `--remote` を付けないとローカルの DB を対象にするため、
このスクリプトには `--remote` が入っています。

同期に対応する前のスキーマでテーブルを作ってしまっている場合は、先に削除してください。

```bash
npm run db:reset
```

### 5. VAPID 鍵を作る

Web Push の送信元を証明する鍵ペアです。

```bash
npm run keys
```

出力された 2 つの値を登録します。順に貼り付けを求められます。

```bash
npm run secrets
```

シークレットを登録すると新しいバージョンが自動で配信されるので、再デプロイは不要です。

### ローカルで動かす場合

```bash
npm run db:init:local    # ローカル DB にテーブルを作る（初回のみ）
npm run dev              # http://127.0.0.1:8787
```

`.dev.vars` をリポジトリ直下に作り、`npm run keys` で作った鍵を書いておきます
（このファイルは Git 管理外）。

```
VAPID_PUBLIC_KEY=B...
VAPID_PRIVATE_KEY=...
```

静的ファイルの配信・API・Cron がすべて 1 つの Worker で動きます。

## 複数端末で同期する

同期サーバは自分でデプロイしたこの Worker で固定です。URL や設定を入力する必要はなく、
**同期コードだけ**でつながります。

1. 1 台目で ⚙ → **複数端末で同期** → **同期を始める**
2. `ABCDE-FGHIJ` の形のコードが表示される
3. 2 台目で ⚙ → 同じ欄にそのコードを入力 → **参加する**

以降はどちらで編集しても、もう一方に反映されます（アプリを開いたとき、画面に戻ったとき、
および表示中は 1 分ごとに確認）。時間割・持ち物・休講・予定のすべてが共有されます。入力時は小文字やハイフン、空白があっても構いません。

- 同時に別々の端末で編集した場合は、**サーバに後から届いた方**が残ります。
  新旧の判定はサーバが振る版番号で行うため、端末の時計がずれていても壊れません。
- 参加すると、その端末の時間割は参加先の内容に置き換わります。
- 「この端末の同期をやめる」を押すと、その端末だけ同期から外れます（他の端末のデータは残ります）。
- 通知を有効にすると自動的に同期も始まります（通知を送るには時間割がサーバ側に必要なため）。

> **同期コードの扱い**: コードを知っている人は誰でもその時間割を読み書きできます。
> 総当たりが現実的でない長さ（30 種類の文字 × 10 桁 ≒ 49 ビット）にしてありますが、
> コードは共有しないでください。紛らわしい文字（0/1/I/L/O/U）は使っていません。

## iPad でアプリとして使う

1. **Safari で**デプロイした URL を開く（他のブラウザではホーム画面に追加しても通知が使えません）
2. 共有ボタン → **ホーム画面に追加**
3. **ホーム画面のアイコンから起動する**
4. 右上の ⚙ → 専攻を選ぶ → **通知を有効にする** → **テスト通知** で受信を確認
5. 他の端末と共有したい場合は、同じ ⚙ の **複数端末で同期** から同期コードを発行する

> **Web Push の条件**: iOS / iPadOS 16.4 以降で、かつ「ホーム画面に追加」したアプリから開いた場合にのみ
> 通知が使えます。Safari のタブで開いている状態では通知を有効にできません。
> 通知の許可はボタンのタップから要求する必要があるため、設定画面のボタン以外では要求しません。

## 仕組み

```
public/            静的ファイル（そのまま配信される）
  js/schedule.js   時程の定義 — フロントと Worker が共有する唯一の情報源
  js/timetable.js  週テンプレート＋日付上書きの解決（同じく共有）
  js/dnd.js        Pointer Events によるドラッグ&ドロップ
  sw.js            Service Worker（キャッシュ / push / notificationclick）
worker/
  index.js         /api/* と Cron Trigger、静的アセットへのフォールバック
  webpush.js       VAPID(ES256) と aes128gcm 暗号化を WebCrypto だけで実装
```

- 時間割は端末の localStorage が正。同期を始めるか通知を有効にした端末だけ、
  時間割と購読情報を D1 に置きます（どちらも使わなければサーバには何も残りません）。
- D1 の `groups` が同期コードごとの時間割、`devices` がそこに属する端末です。
  書き込みのたびに `rev` を 1 つ進め、端末は自分が持つ `rev` と比べて新しければ取り込みます。
- Cron は UTC で `* 23 * * *` と `* 0-6 * * *`、つまり日本時間の 8:40〜15:10 を毎分チェックします。
  実際に送るかどうかは Worker が曜日・時刻・休講設定を見て判定します。日本に夏時間はないため
  UTC+9 の固定オフセットで計算しています。
- 送信先が 404 / 410 を返した購読は自動的に削除します。

### API

| メソッド | パス | 用途 |
|---|---|---|
| GET | `/api/vapid` | VAPID 公開鍵を返す |
| POST | `/api/sync` | 端末を登録する。コードが無ければ発行し、購読情報があれば一緒に保存する |
| DELETE | `/api/sync` | この端末の登録を消す（グループは残る） |
| POST | `/api/join` | 同期コードで参加し、そのグループの時間割を受け取る |
| GET | `/api/state?code=&since=` | 最新の時間割を取る。`since` が現在の版と同じなら本文を省く |
| PUT | `/api/state` | 時間割を書き込み、版番号を 1 つ進める |
| POST | `/api/test` | その端末にテスト通知を送る |
| GET | `/api/dry-run?at=...` | 指定時刻で通知処理を試す（`ALLOW_DRY_RUN="true"` のときだけ） |

## うまくいかないとき

**`binding DB of type d1 must have a valid database_id specified [code: 10021]`**

`wrangler.toml` の `database_id` がプレースホルダーのままです。`deploy` も `secret put` も
アップロード前に設定を検証するため、ここを直すまでどちらも通りません。
`npm run check` で今の状態を確認できます。ID は `npx wrangler d1 list` で調べられます。

よくある原因:

- **メモ帳が `wrangler.toml.txt` として保存している** — 保存時に「ファイルの種類」を
  「すべてのファイル」にするか、VS Code など別のエディタを使ってください
- **別の場所のファイルを編集している** — コマンドを実行しているフォルダの `wrangler.toml` か確認を
- **`[[d1_databases]]` のブロックが 2 つある** — 貼り足すのではなく、既存の行の値だけを置き換えます

**通知が届かない**

- iPadOS 16.4 以上か、ホーム画面のアイコンから起動しているかを確認してください
- 端末の 設定 → 通知 →「時間割」で通知が許可されているか、集中モードで止まっていないか
- 設定画面の「テスト通知」で切り分けられます。テスト通知が届いて授業の通知だけ来ない場合は、
  その曜日・時限に教科が登録されているか、休講にしていないかを確認してください

## 動作確認

```bash
npm test        # 時程・通知文面・同期コードの検証・VAPID 署名・本文の暗号化(RFC 8291 の往復)
```

Cron の動きを手元で試すには、`wrangler.toml` の `ALLOW_DRY_RUN` を `"true"` にして:

```bash
npm run dev
# 月曜 3限(10:50)の10分前 = 日本時間 10:40 = UTC 01:40
curl "http://127.0.0.1:8787/api/dry-run?at=2026-08-31T01:40:00Z"
```

Cron ハンドラ自体を発火させる場合:

```bash
curl "http://127.0.0.1:8787/cdn-cgi/local/scheduled?cron=*+0-6+*+*+*"
```

同期を手元で試す場合:

```bash
CODE=$(curl -s -X POST http://127.0.0.1:8787/api/sync \
  -H 'content-type: application/json' -d '{"deviceId":"a"}' | python3 -c 'import sys,json;print(json.load(sys.stdin)["code"])')
curl -s -X POST http://127.0.0.1:8787/api/join \
  -H 'content-type: application/json' -d "{\"deviceId\":\"b\",\"code\":\"$CODE\"}"
```

アイコンを作り直す場合は `npm run icons`（Python 3 のみ、追加パッケージ不要）。
